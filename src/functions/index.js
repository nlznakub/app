import { normalize as _normalize, scale as _scale, verticalScale as _verticalScale } from "./normalize";


export const normalize = _normalize;
export const scale = _scale;
export const verticalScale = _verticalScale;
export * from './function'